# Section 4.4 – Solving Inequalities Algebraically

> **Course:** MAT 116 — Precalculus  
> **Textbook:** Sullivan *Precalculus* 10th Edition  
> **Section:** 4.4  
> **Generated:** April 11, 2026

---

## Problem 20

**Type:** Inequality Sign Chart  

### Given

> (x - 5)^2 (x + 2) < 0

### Step 1 - Move Everything to One Side

Start with the inequality:
$$ (x - 5)^2·(x + 2) < 0 $$

Move everything to the left so the right side is 0:
$$ (x - 5)^2·(x + 2) < 0 $$

### Step 2 - Factor and State the Domain Restrictions

Numerator factorization: (x - 5)^2·(x + 2)
Denominator factorization: 1
Reduced expression for sign testing: x^3 - 8x^2 + 5x + 50
Domain restriction(s): none

### Step 3 - Find the Critical Numbers

Critical numbers come from zeros of the numerator and denominator.
  x = -2  makes the numerator zero (multiplicity 1)
  x = 5  makes the numerator zero (multiplicity 2)

Place these on a number line in increasing order: -2, 5

### Step 4 - Test the Sign on Each Interval

Use one test value in each interval:
  (-∞, -2)  test x = -3:  value = -64  -> sign −  KEEP
  (-2, 5)  test x = 3/2:  value = 343/8  -> sign +  REJECT
  (5, +∞)  test x = 6:  value = 8  -> sign +  REJECT

### Step 5 - Write the Solution Set

We need the expression to be negative.
Therefore the solution is:
(-∞, -2)

### Final Answer

> Solution: (-∞, -2)

---

## Problem 22

**Type:** Inequality Sign Chart  

### Given

> x^3 + 8x^2 < 0

### Step 1 - Move Everything to One Side

Start with the inequality:
$$ x^3 + 8x^2 < 0 $$

Move everything to the left so the right side is 0:
$$ x^2·(x + 8) < 0 $$

### Step 2 - Factor and State the Domain Restrictions

Numerator factorization: x^2·(x + 8)
Denominator factorization: 1
Reduced expression for sign testing: x^3 + 8x^2
Domain restriction(s): none

### Step 3 - Find the Critical Numbers

Critical numbers come from zeros of the numerator and denominator.
  x = -8  makes the numerator zero (multiplicity 1)
  x = 0  makes the numerator zero (multiplicity 2)

Place these on a number line in increasing order: -8, 0

### Step 4 - Test the Sign on Each Interval

Use one test value in each interval:
  (-∞, -8)  test x = -9:  value = -81  -> sign −  KEEP
  (-8, 0)  test x = -4:  value = 64  -> sign +  REJECT
  (0, +∞)  test x = 1:  value = 9  -> sign +  REJECT

### Step 5 - Write the Solution Set

We need the expression to be negative.
Therefore the solution is:
(-∞, -8)

### Final Answer

> Solution: (-∞, -8)

---

## Problem 28

**Type:** Inequality Sign Chart  

### Given

> x^3 - 2x^2 - 3x > 0

### Step 1 - Move Everything to One Side

Start with the inequality:
$$ x^3 - 2x^2 - 3x > 0 $$

Move everything to the left so the right side is 0:
$$ x·(x^2 - 2x - 3) > 0 $$

### Step 2 - Factor and State the Domain Restrictions

Numerator factorization: x·(x - 3)·(x + 1)
Denominator factorization: 1
Reduced expression for sign testing: x^3 - 2x^2 - 3x
Domain restriction(s): none

### Step 3 - Find the Critical Numbers

Critical numbers come from zeros of the numerator and denominator.
  x = -1  makes the numerator zero (multiplicity 1)
  x = 0  makes the numerator zero (multiplicity 1)
  x = 3  makes the numerator zero (multiplicity 1)

Place these on a number line in increasing order: -1, 0, 3

### Step 4 - Test the Sign on Each Interval

Use one test value in each interval:
  (-∞, -1)  test x = -2:  value = -10  -> sign −  REJECT
  (-1, 0)  test x = -1/2:  value = 7/8  -> sign +  KEEP
  (0, 3)  test x = 3/2:  value = -45/8  -> sign −  REJECT
  (3, +∞)  test x = 4:  value = 20  -> sign +  KEEP

### Step 5 - Write the Solution Set

We need the expression to be positive.
Therefore the solution is:
(-1, 0) ∪ (3, +∞)

### Final Answer

> Solution: (-1, 0) ∪ (3, +∞)

---

## Problem 35

**Type:** Inequality Sign Chart  

### Given

> ((x - 3)(x + 2))/(x - 1) <= 0

### Step 1 - Move Everything to One Side

Start with the inequality:
$$ (x - 3)·(x + 2)/(x - 1) <= 0 $$

Move everything to the left so the right side is 0:
$$ (x - 3)·(x + 2)/(x - 1) <= 0 $$

### Step 2 - Factor and State the Domain Restrictions

Numerator factorization: (x - 3)·(x + 2)
Denominator factorization: x - 1
Reduced expression for sign testing: (x^2 - x - 6)/(x - 1)
Domain restriction(s): x ≠ 1

### Step 3 - Find the Critical Numbers

Critical numbers come from zeros of the numerator and denominator.
  x = -2  makes the numerator zero (multiplicity 1)
  x = 1  makes the denominator zero, so it is excluded from the domain (multiplicity 1)
  x = 3  makes the numerator zero (multiplicity 1)

Place these on a number line in increasing order: -2, 1, 3

### Step 4 - Test the Sign on Each Interval

Use one test value in each interval:
  (-∞, -2)  test x = -3:  value = -3/2  -> sign −  KEEP
  (-2, 1)  test x = -1/2:  value = 7/2  -> sign +  REJECT
  (1, 3)  test x = 2:  value = -4  -> sign −  KEEP
  (3, +∞)  test x = 4:  value = 2  -> sign +  REJECT

### Step 5 - Write the Solution Set

We need the expression to be negative or zero.
Therefore the solution is:
(-∞, -2] ∪ (1, 3]
Include zero(s) of the numerator because the inequality is <=.
Exclude x = 1 because the original expression is undefined there.

### Final Answer

> Solution: (-∞, -2] ∪ (1, 3]

---

## Problem 37

**Type:** Inequality Sign Chart  

### Given

> ((x + 5)^2)/(x^2 - 4) >= 0

### Step 1 - Move Everything to One Side

Start with the inequality:
$$ (x + 5)^2/(x^2 - 4) >= 0 $$

Move everything to the left so the right side is 0:
$$ (x + 5)^2/(x^2 - 4) >= 0 $$

### Step 2 - Factor and State the Domain Restrictions

Numerator factorization: (x + 5)^2
Denominator factorization: (x - 2)·(x + 2)
Reduced expression for sign testing: (x^2 + 10x + 25)/(x^2 - 4)
Domain restriction(s): x ≠ -2, x ≠ 2

### Step 3 - Find the Critical Numbers

Critical numbers come from zeros of the numerator and denominator.
  x = -5  makes the numerator zero (multiplicity 2)
  x = -2  makes the denominator zero, so it is excluded from the domain (multiplicity 1)
  x = 2  makes the denominator zero, so it is excluded from the domain (multiplicity 1)

Place these on a number line in increasing order: -5, -2, 2

### Step 4 - Test the Sign on Each Interval

Use one test value in each interval:
  (-∞, -5)  test x = -6:  value = 1/32  -> sign +  KEEP
  (-5, -2)  test x = -7/2:  value = 3/11  -> sign +  KEEP
  (-2, 2)  test x = 0:  value = -25/4  -> sign −  REJECT
  (2, +∞)  test x = 3:  value = 64/5  -> sign +  KEEP

### Step 5 - Write the Solution Set

We need the expression to be positive or zero.
Therefore the solution is:
(-∞, -2) ∪ (2, +∞)
Include zero(s) of the numerator because the inequality is >=.
Exclude x = -2, 2 because the original expression is undefined there.

### Final Answer

> Solution: (-∞, -2) ∪ (2, +∞)

---

## Problem 40

**Type:** Inequality Sign Chart  

### Given

> (x + 2)/(x - 4) >= 1

### Step 1 - Move Everything to One Side

Start with the inequality:
$$ (x + 2)/(x - 4) >= 1 $$

Move everything to the left so the right side is 0:
$$ 6/(x - 4) >= 0 $$

### Step 2 - Factor and State the Domain Restrictions

Numerator factorization: 6
Denominator factorization: x - 4
Reduced expression for sign testing: 6/(x - 4)
Domain restriction(s): x ≠ 4

### Step 3 - Find the Critical Numbers

Critical numbers come from zeros of the numerator and denominator.
  x = 4  makes the denominator zero, so it is excluded from the domain (multiplicity 1)

Place these on a number line in increasing order: 4

### Step 4 - Test the Sign on Each Interval

Use one test value in each interval:
  (-∞, 4)  test x = 3:  value = -6  -> sign −  REJECT
  (4, +∞)  test x = 5:  value = 6  -> sign +  KEEP

### Step 5 - Write the Solution Set

We need the expression to be positive or zero.
Therefore the solution is:
(4, +∞)
Exclude x = 4 because the original expression is undefined there.

### Final Answer

> Solution: (4, +∞)

---

## Problem 43

**Type:** Inequality Sign Chart  

### Given

> 5/(x - 3) > 3/(x + 1)

### Step 1 - Move Everything to One Side

Start with the inequality:
$$ 5/(x - 3) > 3/(x + 1) $$

Move everything to the left so the right side is 0:
$$ 2·(x + 7)/((x - 3)·(x + 1)) > 0 $$

### Step 2 - Factor and State the Domain Restrictions

Numerator factorization: 2·(x + 7)
Denominator factorization: (x - 3)·(x + 1)
Reduced expression for sign testing: (2x + 14)/(x^2 - 2x - 3)
Domain restriction(s): x ≠ -1, x ≠ 3

### Step 3 - Find the Critical Numbers

Critical numbers come from zeros of the numerator and denominator.
  x = -7  makes the numerator zero (multiplicity 1)
  x = -1  makes the denominator zero, so it is excluded from the domain (multiplicity 1)
  x = 3  makes the denominator zero, so it is excluded from the domain (multiplicity 1)

Place these on a number line in increasing order: -7, -1, 3

### Step 4 - Test the Sign on Each Interval

Use one test value in each interval:
  (-∞, -7)  test x = -8:  value = -2/77  -> sign −  REJECT
  (-7, -1)  test x = -4:  value = 2/7  -> sign +  KEEP
  (-1, 3)  test x = 1:  value = -4  -> sign −  REJECT
  (3, +∞)  test x = 4:  value = 22/5  -> sign +  KEEP

### Step 5 - Write the Solution Set

We need the expression to be positive.
Therefore the solution is:
(-7, -1) ∪ (3, +∞)
Exclude x = -1, 3 because the original expression is undefined there.

### Final Answer

> Solution: (-7, -1) ∪ (3, +∞)

---

## Problem 44

**Type:** Inequality Sign Chart  

### Given

> 1/(x - 2) < 2/(3x - 9)

### Step 1 - Move Everything to One Side

Start with the inequality:
$$ 1/(x - 2) < 2/(3x - 9) $$

Move everything to the left so the right side is 0:
$$ (x - 5)/(3·(x - 3)·(x - 2)) < 0 $$

### Step 2 - Factor and State the Domain Restrictions

Numerator factorization: x - 5
Denominator factorization: 3·(x - 3)·(x - 2)
Reduced expression for sign testing: (x - 5)/(3x^2 - 15x + 18)
Domain restriction(s): x ≠ 2, x ≠ 3

### Step 3 - Find the Critical Numbers

Critical numbers come from zeros of the numerator and denominator.
  x = 2  makes the denominator zero, so it is excluded from the domain (multiplicity 1)
  x = 3  makes the denominator zero, so it is excluded from the domain (multiplicity 1)
  x = 5  makes the numerator zero (multiplicity 1)

Place these on a number line in increasing order: 2, 3, 5

### Step 4 - Test the Sign on Each Interval

Use one test value in each interval:
  (-∞, 2)  test x = 1:  value = -2/3  -> sign −  KEEP
  (2, 3)  test x = 5/2:  value = 10/3  -> sign +  REJECT
  (3, 5)  test x = 4:  value = -1/6  -> sign −  KEEP
  (5, +∞)  test x = 6:  value = 1/36  -> sign +  REJECT

### Step 5 - Write the Solution Set

We need the expression to be negative.
Therefore the solution is:
(-∞, 2) ∪ (3, 5)
Exclude x = 2, 3 because the original expression is undefined there.

### Final Answer

> Solution: (-∞, 2) ∪ (3, 5)

---
